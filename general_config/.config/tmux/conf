set -ga terminal-overrides ',st-256color:Tc'
set -g default-terminal 'tmux-256color' # replace this with your custom term-info name
set -ga terminal-overrides ',*:Tc' # this is for 256 color
set -ga terminal-overrides '*:Ss=\E[%p1%d q:Se=\E[ q' # this is for the cursor shape
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix
bind r source-file ~/.tmux.conf
bind v split-window -h
bind - split-window -v
unbind '"'
unbind %
set -g status-bg default
set -g status-fg red
#setw -g window-status-current-bg default
#setw -g window-status-current-fg white
#set -g status-right '#[fg=green]][#[fg=white] #T #[fg=green]][ #[fg=blue]%Y-%m-%d #[fg=white]%H:%M#[default]'
set-window-option -g xterm-keys on # for vim
set-window-option -g mode-keys vi # vi key
#set-window-option -g monitor-activity on
bind Space list-panes
#set-option -g status-left '#[fg=magenta]» #[default]'
#set-option -g status-right '#[fg=red]%R '
set-window-option -g mode-keys vi
unbind l
#bind h select-pane -L
#bind j select-pane -D
#bind k select-pane -U
#bind l select-pane -R
unbind q
bind q kill-pane

bind -r C-h run "tmux select-pane -L"
bind -r C-j run "tmux select-pane -D"
bind -r C-k run "tmux select-pane -U"
bind -r C-l run "tmux select-pane -R"
bind -r C-\ run "tmux select-pane -l"
set -g status on

# Human numbering
set -g base-index 1

#set -g set-titles on
#set-option -g set-titles-string '#{pane_current_command}'
#set-option -g history-limit 30000
#set-option -g visual-activity on
#set-window-option -g monitor-activity on

# Change the default $TERM to screen-256color
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ',*256col*:Tc'

# No bells at all
set -g bell-action none

# Change the prefix key to C-a
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Shell
set -g default-command /bin/zsh
set -g default-shell /bin/zsh

# Status
#set -g status-left ''
#set -g status-right '#[fg=blue,bg=red]#[fg=black,bg=red]  #(cut -d " " -f 1-3 /proc/loadavg) #[fg=black,bg=red] #[fg=white,bg=black]#[fg=magenta]  #[fg=yellow]%B#[fg=green] %d, #[fg=blue]%R '
#set -g status-right-length 100
#set -g status-bg black

# Window status
#set-option -g status-position bottom
#setw -g window-status-format '#[fg=white,bold]#[fg=white,bg=red] #I #[fg=blue,bg=white]#[fg=blue,bg=white] #W '
#setw -g window-status-current-format '#[fg=red]#[fg=white,bg=black] #I #[fg=cyan,bg=black]#[fg=white,bg=black] #W '

# Enable mouse
set-option -g mouse on


# Clear pane history

# Pane creation

# Pane management

# Movement vi-like
bind-key -r k select-pane -U
bind-key -r j select-pane -D
bind-key -r h select-pane -L
bind-key -r l select-pane -R

# Movement normal :)

# Reload conf


# Copy | require xclip
bind-key -T copy-mode-vi 'v' send -X begin-selection
#bind-key -T copy-mode-vi 'y' send -X copy-pipe "xclip"
bind-key -T copy-mode-vi 'y' send-keys -X copy-pipe-and-cancel "xclip -i -sel clip > /dev/null"

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
#is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    #| grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
# Erase default options

# Colour
#COLOUR_BG="#161325"

#set-option -g status-position top
#setw -g window-status-format "#[fg=black,bg=white] #I | #[fg=black,bg=white] #W"
#setw -g window-status-current-format "#[fg=black,bg=white] #I | #[fg=black,bg=white] #W"
#setw -g window-status-format "#[fg=white,bg=#1D1F21]#[fg=black,bg=white] #I #[fg=black,bg=white] #W #[fg=white,bg=#1D1F21]"
setw -g window-status-current-format "#[fg=white,bg=#1D1F21]#[bg=white]#[fg=black,bg=white] #I #[fg=black,bg=white] #{pane_current_path} #[fg=black,bg=white] #W #[fg=white ,bg=#1D1F21]"
set -g status-left "#[fg=red,bg=red]"
set -g status-left "#[fg=black,bg=black]#[fg=white,bg=black] #S #[fg=white,bg=black,nobold,nounderscore,noitalics]>> [fg=black,bg=black]"
set -g status-right '#[fg=colour14]  '
set -g status-justify left
set -g pane-active-border-style "fg=black,bg=default"
set -g status-style "fg=default,bg=default"

#setw -g window-status-activity-attr "none"
#setw -g window-status-activity-style "fg=colour27,bg=colour234,none"
#setw -g window-status-separator " "
#setw -g window-status-style "fg=colour39,bg=colour234,none"
#set -g pane-active-border-style "fg=$COLOUR_BG" # hide border
#set -g pane-border-style "fg=$COLOUR_BG" # hide border

